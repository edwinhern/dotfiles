# Files prettier should skip when run from the repo root.
# Mirrors the exclusions previously inline in scripts/lint.sh + format.sh.

# Vendored bats companion libraries (third-party, not our style).
tests/test_helpers/

# Chezmoi templates: contain Go template syntax (`{{ ... }}`) which prettier
# cannot parse as YAML, Markdown, or JSON.
*.tmpl

# Build artifacts and runtime state.
.git/
.worktrees/
